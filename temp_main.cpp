/*
#include "Dialogue.h"

int main(void){
    int number = 4;

    bool line_is_read = Game::Dialogue::readLine(number);

    return 0;

}
*/

#include <iostream>
#include <fstream>
#include <string>


/*
int main() {
    std::string filename = "NPCDialogues.txt"; // Set the filename

    // Open the file
    std::ifstream file(filename);

    // Check if the file was successfully opened
    if (!file.is_open()) {
        std::cerr << "Failed to open the file." << std::endl;
        return 1; // Exit with an error code
    }

    // Read and print the content of the file line by line
    std::string line;
    while (std::getline(file, line)) {
        std::cout << line << std::endl;
    }

    // Close the file
    file.close();

    return 0; // Exit successfully
}
*/


